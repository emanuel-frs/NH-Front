import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    logo: {
        width: '100%',
        alignItems: "center",
        marginBottom: 10,
        paddingHorizontal: 20
    },
    logoImg: {
        width: 250,
        height: 250
    }
});