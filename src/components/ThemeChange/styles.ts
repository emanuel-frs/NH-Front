import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    buttonContainer: {
      position: 'absolute',
      top: 10,
      right: 0,
      zIndex: 1000,
    },
    button: {
      paddingVertical: 10,
      paddingHorizontal: 20,
      borderRadius: 10,
    },
    iconTheme: {
        width: 40,
        height: 40
    },
  });